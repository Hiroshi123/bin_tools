
# bipatch 

bipatch is a binary(hex) patch command.
This is intented to be used for insertion & deletion of binary instructions.

# Patch format

When you insert binary from one address,
Command(1) + Start Address + Byte Sequence

When you delete binary from one address,
Command(0) + Start Address + number of deleted sequence 

